# 🔍 Диагностика проблемы "Сервер недоступен"

## ✅ ИСПРАВЛЕНО

### 1. **Проблема с `checkServerStatus` - невалидный параметр `timeout`**
   - **Было**: `timeout: 5000` в опциях fetch (не работает в React Native)
   - **Исправлено**: Использован `AbortController` с таймаутом 5 секунд
   - **Файл**: `app/(tabs)/index.tsx` (строки 85-104)

### 2. **Модель DeepSeek API**
   - **Было**: `deepseek-reasoner`
   - **Исправлено**: `deepseek-chat` (DeepSeek R1)
   - **Файл**: `server.py` (строка 187)

### 3. **CORS настройки**
   - **Было**: Базовый CORS без обработки OPTIONS
   - **Исправлено**: 
     - Расширенные CORS настройки
     - Обработка preflight запросов (OPTIONS)
     - Правильные заголовки для мобильных устройств
   - **Файл**: `server.py` (строки 72-80, 452-475, 286-295)

### 4. **Обработка ошибок**
   - **Добавлено**: Детальное логирование ошибок сети
   - **Добавлено**: Различение типов ошибок (таймаут, сеть, HTTP)
   - **Файл**: `app/(tabs)/index.tsx` (строки 85-120)

### 5. **Логирование**
   - **Добавлено**: Логирование URL запросов
   - **Добавлено**: Логирование статусов ответов
   - **Файл**: `app/(tabs)/index.tsx`, `server.py`

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ (требуют проверки)

### 1. **IP адрес сервера**
   - **Текущий**: `192.168.0.10:5000`
   - **Проверьте**:
     ```bash
     # На компьютере с сервером выполните:
     ipconfig  # Windows
     # или
     ifconfig  # Linux/Mac
     ```
   - Найдите ваш локальный IP (обычно начинается с `192.168.x.x` или `10.0.x.x`)
   - Убедитесь, что телефон и компьютер в **одной Wi-Fi сети**

### 2. **Запуск сервера**
   - Убедитесь, что `server.py` запущен:
     ```bash
     cd PhysicsSolver
     python server.py
     ```
   - Должно появиться:
     ```
     🚀 Запуск Physics Solver Server...
     📡 API эндпоинт: http://0.0.0.0:5000/solve
     * Running on http://127.0.0.1:5000
     ```

### 3. **Firewall/Брандмауэр**
   - Windows может блокировать порт 5000
   - Разрешите Python через брандмауэр или отключите его временно для теста

### 4. **Эмулятор vs Реальное устройство**
   - **Эмулятор Android**: Используйте `10.0.2.2` вместо `192.168.0.10`
   - **Реальное устройство**: Используйте реальный IP компьютера
   - **iOS Simulator**: Используйте `localhost` или `127.0.0.1`

---

## 🧪 ТЕСТИРОВАНИЕ

### Шаг 1: Проверка сервера в браузере
   Откройте в браузере на компьютере:
   ```
   http://localhost:5000/status
   ```
   Должен вернуться JSON:
   ```json
   {
     "success": true,
     "api_key_valid": true,
     "ocr_available": true,
     "server_time": "12:34:56"
   }
   ```

### Шаг 2: Проверка с телефона
   На телефоне в браузере откройте:
   ```
   http://192.168.0.10:5000/status
   ```
   (Замените `192.168.0.10` на ваш реальный IP)

### Шаг 3: Проверка в приложении
   - Откройте приложение
   - Должен появиться индикатор статуса
   - Если "Сервер недоступен" - проверьте IP адрес

---

## 📝 ЧТО ИЗМЕНИТЬ В КОДЕ (если нужно)

### Изменить IP адрес в приложении:
   **Файл**: `app/(tabs)/index.tsx` (строка 35)
   ```typescript
   const SERVER_URL = `http://ВАШ_IP:5000/solve`;
   ```

### Для эмулятора Android:
   ```typescript
   const SERVER_URL = Platform.OS === 'android' 
     ? `http://10.0.2.2:5000/solve`  // Эмулятор
     : `http://192.168.0.10:5000/solve`;  // Реальное устройство
   ```

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ УЛУЧШЕНИЯ

### Если проблема сохраняется:

1. **Проверьте логи сервера**
   - В терминале где запущен `server.py` должны появляться логи при запросах

2. **Проверьте логи приложения**
   - В Metro Bundler или Expo Go должны быть логи
   - Ищите строки с `🔍`, `📡`, `❌`

3. **Проверьте API ключ DeepSeek**
   - Убедитесь, что ключ в `server.py` правильный
   - Проверьте баланс на https://platform.deepseek.com

4. **Проверьте OCR**
   - Убедитесь, что Tesseract установлен
   - Путь в `server.py` (строка 26) должен быть правильным

---

## ✅ РЕЗЮМЕ ИСПРАВЛЕНИЙ

1. ✅ Исправлен `checkServerStatus` - теперь использует `AbortController`
2. ✅ Изменена модель на `deepseek-chat` (R1)
3. ✅ Улучшены CORS настройки
4. ✅ Добавлено детальное логирование
5. ✅ Улучшена обработка ошибок

**Следующий шаг**: Проверьте IP адрес и убедитесь, что сервер запущен!

